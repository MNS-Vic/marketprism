# 简单的NATS Server配置文件
# 启用JetStream功能

# 服务器基本配置
port: 4222

# JetStream配置
jetstream {
    store_dir: "/tmp/nats-jetstream"
    max_memory_store: 512MB
    max_file_store: 2GB
}

# 监控配置
http_port: 8222

# 日志配置
debug: true
trace: false