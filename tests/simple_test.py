#!/usr/bin/env python3\nprint(\"开始简单测试...\")\n\nimport sys\nimport os\nprint(f\"当前目录: {os.getcwd()}\")\nprint(f\"Python路径: {sys.path[:3]}\")\n\n# 添加路径\npath_to_add = '../services/python-collector/src'\nfull_path = os.path.abspath(path_to_add)\nprint(f\"要添加的路径: {full_path}\")\nprint(f\"路径是否存在: {os.path.exists(full_path)}\")\n\nsys.path.insert(0, full_path)\n\n# 尝试导入\nprint(\"尝试导入...\")\ntry:\n    from marketprism_collector.collector import MarketDataCollector\n    print(\"✅ 导入成功!\")\nexcept Exception as e:\n    print(f\"❌ 导入失败: {e}\")\n    import traceback\n    traceback.print_exc()\n\nprint(\"测试完成\")\n"